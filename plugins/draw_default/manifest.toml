interface_name = "draw"
interface_version = "1"
plugin_name = "default"
module_path = "${build_dir}/draw_default.dll"
supported_lifetimes = ["singleton"]
init = true
shutdown = true

[dependencies]
logger = {}
renderer = {}
render_graph = {}
allocator = {}
